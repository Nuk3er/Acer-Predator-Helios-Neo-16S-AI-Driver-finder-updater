<Page
  x:Class="HeliosToolkit.App.Views.Pages.WindowsTweaksPage"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
  xmlns:controls="clr-namespace:HeliosToolkit.App.Views.Controls"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  mc:Ignorable="d"
  d:DesignHeight="800"
  d:DesignWidth="1100"
  Title="Windows Tweaks"
  Foreground="{DynamicResource TextFillColorPrimaryBrush}">

  <Page.Resources>
    <BooleanToVisibilityConverter x:Key="BoolToVis" />
  </Page.Resources>

  <Grid Margin="32,24,20,0">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <Grid Grid.Row="0" Margin="0,0,12,0">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>
      <StackPanel Grid.Column="0">
        <TextBlock Style="{StaticResource PageTitleTextStyle}" Text="Windows Tweaks" />
        <TextBlock
          Margin="0,4,0,0"
          Style="{StaticResource PageSubtitleTextStyle}"
          Text="Every change is labeled by risk, detected live, and fully reversible. Read the descriptions — some popular tweaks do nothing on modern Windows 11." />
      </StackPanel>
      <ui:Button
        Grid.Column="1"
        VerticalAlignment="Top"
        Command="{Binding ViewModel.LoadCommand}"
        Content="Re-detect"
        Icon="{ui:SymbolIcon ArrowClockwise24}" />
    </Grid>

    <Grid Grid.Row="1" Margin="0,14,0,0">
      <ui:ProgressRing
        VerticalAlignment="Top"
        HorizontalAlignment="Center"
        Margin="0,30,0,0"
        IsIndeterminate="True"
        Visibility="{Binding ViewModel.IsLoading, Converter={StaticResource BoolToVis}}" />
      <ScrollViewer Padding="0,0,12,24" VerticalScrollBarVisibility="Auto">
        <controls:TweakListView DataContext="{Binding ViewModel.GroupedTweaks}" />
      </ScrollViewer>
    </Grid>
  </Grid>
</Page>
